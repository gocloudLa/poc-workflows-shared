name: Create Release

on:
  workflow_call:
    inputs:
      tag_name:
        description: 'Tag name to create release for (e.g., v1.2.0)'
        required: true
        type: string
      release_notes:
        description: 'Release notes (optional). If empty, uses CHANGELOG.md from the tag.'
        required: false
        type: string
        default: ''
      skip_execution:
        required: false
        type: boolean
        default: false

jobs:
  create-release:
    name: Create GitHub Release
    if: ${{ !inputs.skip_execution }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    environment:
      name: release
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Verify tag exists
        run: |
          if ! git rev-parse "${{ inputs.tag_name }}" >/dev/null 2>&1; then
            echo "❌ Tag ${{ inputs.tag_name }} does not exist"
            echo "Available tags:"
            git tag -l | tail -10
            exit 1
          fi
          echo "✅ Tag ${{ inputs.tag_name }} exists"

      - name: Extract changelog from tag
        id: changelog
        run: |
          set -euo pipefail
          CHANGELOG_PATH="$RUNNER_TEMP/changelog-${{ inputs.tag_name }}.md"
          
          if git cat-file -e "${{ inputs.tag_name }}:CHANGELOG.md" 2>/dev/null; then
            git show "${{ inputs.tag_name }}:CHANGELOG.md" > "$CHANGELOG_PATH"
            echo "✅ Loaded CHANGELOG.md from tag"
          elif git cat-file -e "${{ inputs.tag_name }}:changelog.md" 2>/dev/null; then
            git show "${{ inputs.tag_name }}:changelog.md" > "$CHANGELOG_PATH"
            echo "✅ Loaded changelog.md from tag"
          else
            : > "$CHANGELOG_PATH"
            echo "⚠️ No changelog found in tag"
          fi
          
          echo "path=$CHANGELOG_PATH" >> "$GITHUB_OUTPUT"

      - name: Check if release already exists
        id: check-release
        run: |
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/releases/tags/${{ inputs.tag_name }}")
          
          if [ "$RESPONSE" = "200" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "⚠️ Release for tag ${{ inputs.tag_name }} already exists"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "✅ No existing release found for tag ${{ inputs.tag_name }}"
          fi

      - name: Create GitHub Release
        if: steps.check-release.outputs.exists == 'false'
        run: |
          RELEASE_NOTES="${{ inputs.release_notes }}"
          if [ -z "$RELEASE_NOTES" ]; then
            if [ -s "${{ steps.changelog.outputs.path }}" ]; then
              RELEASE_NOTES="$(cat "${{ steps.changelog.outputs.path }}")"
            else
              RELEASE_NOTES="Automated release created after successful staging validation."
            fi
          fi
          
          RELEASE_BODY=$(cat <<EOF
          $RELEASE_NOTES
          
          **Deployment Information:**
          - Tag: ${{ inputs.tag_name }}
          - Created: $(date -u +"%Y-%m-%d %H:%M:%S UTC")
          
          This release will trigger the production deployment workflow.
          EOF
          )
          
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/releases" \
            -d "{
              \"tag_name\": \"${{ inputs.tag_name }}\",
              \"name\": \"Release ${{ inputs.tag_name }}\",
              \"body\": $(echo "$RELEASE_BODY" | jq -Rs .),
              \"draft\": false,
              \"prerelease\": false
            }"
          
          echo "✅ Release created successfully"

      - name: Output release information
        run: |
          if [ "${{ steps.check-release.outputs.exists }}" = "false" ]; then
            echo "✅ Release created successfully: ${{ inputs.tag_name }}"
            echo "🚀 Production deployment will be triggered automatically"
          else
            echo "ℹ️ Release already exists, skipping creation"
          fi
