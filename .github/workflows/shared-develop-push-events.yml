name: "Shared Develop Push Events"

on:
  workflow_call:
    inputs:
      # AWS Configuration
      ecr_repository_url:
        description: 'ECR repository URL'
        required: true
        type: string
      aws_role_arn_shared:
        description: 'AWS IAM role ARN for ECR access'
        required: true
        type: string
      aws_role_arn_dev:
        description: 'AWS IAM role ARN for dev environment'
        required: true
        type: string
      aws_region:
        description: 'AWS region'
        required: false
        type: string
        default: 'us-east-1'
      
      # Service configuration
      service_name:
        description: 'Service name'
        required: false
        type: string
        default: ''
      
      # Skip flags
      skip_security_scan:
        required: false
        type: boolean
        default: false
      skip_build:
        required: false
        type: boolean
        default: false
      skip_deploy:
        required: false
        type: boolean
        default: false
      skip_deploy_verification:
        required: false
        type: boolean
        default: false
      skip_health_check:
        required: false
        type: boolean
        default: false

concurrency:
  group: shared-develop-push-events-${{ github.ref }}
  cancel-in-progress: true

jobs:
  security-scan:
    uses: ./.github/workflows/security-scan.yml
    with:
      skip_execution: ${{ inputs.skip_security_scan }}
    secrets: inherit

  build-and-push:
    needs: security-scan
    uses: ./.github/workflows/build-and-push.yml
    with:
      ecr_repository_url: ${{ inputs.ecr_repository_url }}
      aws_role_arn: ${{ inputs.aws_role_arn_shared }}
      aws_region: ${{ inputs.aws_region }}
      service_name: ${{ inputs.service_name }}
      branch: develop
      skip_execution: ${{ inputs.skip_build }}
    secrets: inherit

  deploy-dev:
    needs: build-and-push
    uses: ./.github/workflows/deploy-dev.yml
    with:
      ecr_repository_url: ${{ inputs.ecr_repository_url }}
      aws_role_arn: ${{ inputs.aws_role_arn_dev }}
      aws_region: ${{ inputs.aws_region }}
      service_name: ${{ inputs.service_name }}
      image_tag: ${{ needs.build-and-push.outputs.image_tag }}
      skip_execution: ${{ inputs.skip_deploy }}
    secrets: inherit

  deploy-verification:
    needs: deploy-dev
    uses: ./.github/workflows/deploy-verification.yml
    with:
      service_name: ${{ inputs.service_name }}
      environment: dev
      skip_execution: ${{ inputs.skip_deploy_verification }}
    secrets: inherit

  health-check:
    needs: deploy-verification
    uses: ./.github/workflows/health-check.yml
    with:
      service_name: ${{ inputs.service_name }}
      environment: dev
      image_tag: ${{ needs.build-and-push.outputs.image_tag }}
      skip_execution: ${{ inputs.skip_health_check }}
    secrets: inherit

  workflow-summary:
    if: always()
    runs-on: ubuntu-latest
    needs: [security-scan, build-and-push, deploy-dev, deploy-verification, health-check]
    steps:
      - name: Workflow Summary
        run: |
          echo "## Develop Branch Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Security Scan | ${{ needs.security-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build & Push | ${{ needs.build-and-push.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Deploy to Dev | ${{ needs.deploy-dev.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Deploy Verification | ${{ needs.deploy-verification.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Health Check | ${{ needs.health-check.result }} |" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}" = "true" ]; then
            echo "❌ Deployment failed or was cancelled"
            exit 1
          else
            echo "✅ Deployment completed successfully"
          fi

